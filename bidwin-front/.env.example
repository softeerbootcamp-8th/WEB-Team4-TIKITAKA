# 실제 값은 .env.local(git 추적 제외)에 넣는다. 비밀 값은 이 파일에 적지 않는다.

# API 베이스 URL. 비워두면 같은 오리진으로 요청하고,
# 개발 서버에서는 vite.config.ts의 /api 프록시가 백엔드로 넘긴다.
VITE_API_BASE_URL=

# 개발 서버 /api 프록시 대상. 비워두면 http://localhost:8080 을 쓴다.
VITE_DEV_API_PROXY_TARGET=
