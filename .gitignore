# OS / editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# root tooling (husky/commitlint deps live only at repo root)
/node_modules

# per-machine Claude Code settings, never shared
**/.claude/settings.local.json
