{
  "name": "bidwin",
  "private": true,
  "version": "0.0.0",
  "description": "root tooling only (git hooks, commit/PR lint) — app code lives in bidwin-front / bidwin-back",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "husky": "^9.1.7"
  }
}
